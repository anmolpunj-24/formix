name: Deploy Project

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    container:
      image: node:24
    steps:
        - uses: actions/checkout@v4
        - run: npm ci
        - run: npm run build


